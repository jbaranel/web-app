export class User {
    constructor(username, firstName, lastName, email, createdAt) {
        this.username = username,
        this.firstName = firstName,
        this.lastName = lastName,
        this.email = email,
        this.createdAt = createdAt
    }
  }

